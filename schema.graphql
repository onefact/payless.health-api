type EmployerIdentificationNumber {
  name: String!
  id: ID!
}

type NationalProviderIdentifier {
  id: ID!
}

type StandardChargeFile {
  url: String!
}

type Hospital {
  name: String!
  ein: EmployerIdentificationNumber!
  npis(ids: [ID!]): [NationalProviderIdentifier!]
  zip: Int!
  standardChargeFiles(urls: [String!]
}

